export function get_char_at() {
  return "foo".charAt;
}

export class Rectangle {
  constructor(x, y){
    this.x = x,
    this.y = y
  }

  static eq(x, y) {
    return x === y;
  }
}

export class Rectangle2 {
  constructor(x, y){
    this.x = x,
    this.y = y
  }

  static eq(x, y) {
    return x === y;
  }
}
